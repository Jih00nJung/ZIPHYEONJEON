api 받아오는건 global api 폴더에서 만들기
그리고 그걸 조회하는 기능은 내 price_search 폴더에 기능만들기 
프론트는 react.js로 만들기
.env는 glbal 안에 만들기
PriceSearchController.java
기능(오늘 구현 4개):

주소 정규화: GET /api/price-search/address/normalize

국토부 실거래가: GET /api/price-search/trades/molit

서울시 실거래가: GET /api/price-search/trades/seoul

개별공시지가: GET /api/price-search/official-land-price

Controller는 요청/응답만 담당하고 로직은 Service로.

PriceSearchService.java

기능:

normalize(address) 호출해서 표준키 생성

키로 MolitRealTradeClient / SeoulRealTradeClient / VworldOfficialLandPriceClient 호출

외부 결과를 “내부 표준 DTO”로 변환해서 반환

PriceSearch 내부 DTO 묶음 (프론트/다른 팀원이 쓰기 쉽게 통일)

📁 PriceSearch/dto/request/ (요청 DTO)

MolitTradeSearchRequest.java

기능: (선택) dealType/buildingType/yyyymm 같은 요청값 묶음

SeoulTradeSearchRequest.java

기능: (선택) gu_name/start/end/dealType

OfficialLandPriceRequest.java

기능: (선택) year 등

AddressNormalizeRequest.java

기능: (선택) address 파라미터 묶음 (GET면 없어도 됨)

📁 PriceSearch/dto/response/ (응답 DTO)

NormalizedAddressResponse.java

기능: 표준화 결과(시도/시군구/법정동/지번/pnu/좌표 등)

TradeItemResponse.java

기능: 실거래 1건을 통일 포맷으로 (날짜/면적/금액/출처)

TradeListResponse.java

기능: 거래 리스트 + 조회 조건/출처 메타

OfficialLandPriceResponse.java

기능: 공시지가 결과(기준년도, 금액, pnu 등)

한 줄 요약(오늘 작업 흐름)

PriceSearchService

address 받음
→ VworldGeocodeClient로 정규화
→ 정규화 결과로

MolitRealTradeClient 호출(전국)

SeoulRealTradeClient 호출(서울)

VworldOfficialLandPriceClient 호출(공시지가)
→ 내부 표준 DTO로 반환 ----------------------------------------2026.01.07